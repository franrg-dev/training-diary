export default function NutricionComidas({ tituloDropdown }) {
  return (
    <div style={{ padding: '20px 16px 0' }}>
      {tituloDropdown}
      <p style={{ marginTop: '24px', color: 'var(--color-texto-secundario)', fontSize: '14px' }}>
        Próximamente — biblioteca de comidas
      </p>
    </div>
  )
}
